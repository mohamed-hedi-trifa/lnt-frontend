export default interface IPartnerPreviousEdition {
    id?: number; // Assuming there is an ID field
    previous_edition_id: number;
    partner_id: number;
    created_at?: string;
    updated_at?: string;
}
